<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="de.berlindroid.zeaapp">
    <!--
        HEY DUDE, YOU ARRIVED HERE :D
        I have no idea what I'm doing, just don't touch the manifest as is idk
    -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera.ar"
        android:required="false" />
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".WorkoutActivity"
            android:label="@string/title_activity_workout"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".SellYourSoulActivity"
            android:label="@string/title_activity_sell_your_soul"
            android:theme="@style/AppTheme.NoActionBar" />

        <meta-data
            android:name="com.google.ar.core"
            android:value="optional" />

        <activity android:name=".PokemonDetailsActivity" />
        <activity android:name=".WhereAreChetAndRomainActivity" />
        <activity android:name=".AnimateMeEverywhere" />
        <activity android:name=".LuckySplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AnimalActivity" />
        <activity
            android:name=".SplashActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LogoActivity"
            android:theme="@style/Theme.AppCompat" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".emails.EmailActivity" />
        <activity android:name=".IamTheRealSplashActivity" />
        <activity android:name=".PokemonActivity" />
        <activity android:name=".WebviewActivity" />
        <activity android:name=".PaletteActivity" />
        <activity android:name="helloar.HelloArActivity" />
        <activity android:name=".BallingActivity" />
    </application>

</manifest>